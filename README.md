f
========================




```bash
~:     $ cd ~/src
~/src: $ f --add # add ~/src to favorite_directories.
input abbr name. if you skip, it will be favorites>

~/src: $ cd ~
~:     $ > f
0: scripts: ~/src/scripts/
1: src: ~/src/
select a path > 1 #  run pushd ~/src
~/src: $

~/src: $ f s # If you press tab key, f complete s
scripts src
~/src: $ f sc # If you press tab key, sc becomes scripts
```
